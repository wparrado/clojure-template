(ns clojure-template.unit.application.item-handlers-test)
;; Tests moved to clojure-template.integration.application.item-handlers-test
;; because they use real infrastructure adapters (in-memory repository, clock, etc.)
